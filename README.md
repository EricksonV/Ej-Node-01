# Especificaciones

```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     Es la base para la tabla de multiplicar y crear el archivo
                                                             [number] [required]
  -l, --listar   Mostrar la tabla en consola          [boolean] [default: false]
  -h, --hasta    Hasta que numero se desea llegar         [number] [default: 10]
```